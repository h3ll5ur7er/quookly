"""Data shapes passed between layers.

Depends on nothing else in the package. This is what allows engines to stay pure and
keeps layers from importing one another merely to share a type definition.
"""
