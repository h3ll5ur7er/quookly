
# Coverage data, written by every `just coverage` run.
.coverage
.coverage.*
coverage.xml
htmlcov/

# An instance's own configuration, including its signing key. `.env.example` is the one
# that is committed.
.env
